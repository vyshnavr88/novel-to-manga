# requirements.txt - FIXED VERSION (No Rust compilation needed!)
# This uses pre-compiled wheels that work on all platforms

fastapi==0.104.1
uvicorn[standard]==0.24.0
groq==0.4.2
httpx==0.25.2
python-dotenv==1.0.0

# Use Pydantic v1 instead of v2 (v2 requires Rust compilation)
# v1 is stable, mature, and works perfectly for our needs
pydantic==1.10.13

# Alternative: If you really want Pydantic v2, use these specific versions
# that have pre-built wheels for Python 3.11+
# pydantic==2.9.2
# pydantic-core==2.23.4
# annotated-types==0.7.0